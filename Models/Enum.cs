public class Enum
{
    public enum Gender
    {
        Male,
        Female,
    }
    public enum SupplierStatus
    {
        Active,
        Inactive,

    }
    public enum PurchaseRequestStatus
    {
        Pending,
        Approved,
        Rejected,
    }
    public enum AccountStatus
    {
        Active,
        Inactive,

    }

    public enum UOM
    {
        KG,
        Litter,
        Meter
    }
}